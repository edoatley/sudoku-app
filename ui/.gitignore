# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
playwright-report
test-results
coverage
tests/integration/auth-state.json
# Coach-quality run outputs (per-scenario JSON/MD traces and the per-label summary.txt) are all
# ignored — large, vary run-to-run from model non-determinism, no lasting value beyond debugging
# that session. Curated baselines worth diffing an incumbent against are copied by hand into
# tests/coach-quality/baselines/ (tracked), keeping the noisy run dirs out of git.
tests/coach-quality/reports/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
